# 生产环境必须改。可以用下面命令生成：
# python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
APP_ENCRYPTION_KEY=

# 可选，默认 data/app.sqlite3
MCFINDER_DB=

# 可选：native/cubiomes 查询并行度。默认 min(8, CPU 核心数 - 1)
MC_QUERY_WORKERS=

# 可选：每个 native batch subprocess 承载的查询数。默认 64，范围 1-512
MC_QUERY_BATCH_SIZE=
